<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Ficha do Visitante</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f4;
      margin: 0; padding: 0;
    }
    .titulo-prioridades {
      text-align: center; margin: 30px 0 0;
      line-height: 1.2;
    }
    .titulo-prioridades .vermelho { color: #c8102e; font-weight: bold; font-size: 32px; }
    .titulo-prioridades .cinza { color: #333; font-size: 32px; }
    .titulo-prioridades .bold { font-weight: bold; font-size: 42px; }
    .container {
      background: #fff; padding: 30px; border-radius: 12px;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
      max-width: 600px; margin: 20px auto 40px;
    }
    h1 { text-align: center; color: #333; }
    label { font-weight: bold; display: block; margin: 20px 0 5px; }
    .imagem { width: 100%; display: block; margin: 20px auto; }
    button {
      display: block; width: 100%; padding: 12px;
      background: #007BFF; color: #fff; font-size: 16px;
      border: none; border-radius: 6px; cursor: pointer; margin-top: 20px;
    }
    button:hover { background: #0056b3; }
    .resposta-aluno {
      background: #fff3cd; border:1px solid #ffeeba;
      padding: 8px; border-radius: 6px; margin-bottom: 10px;
      font-weight: bold; color: #856404;
    }
    textarea {
      width: 100%; padding: 8px; margin-bottom: 15px;
      border: 1px solid #ccc; border-radius: 6px;
      resize: vertical; min-height: 80px;
    }
  </style>
</head>
<body>
  <div class="titulo-prioridades">
    <span class="vermelho">ENUMERE</span>
    <span class="cinza">SUAS</span><br>
    <span class="cinza bold">PRIORIDADES</span>
  </div>

  <div class="container">
    <h1>Ficha do Visitante</h1>
    <form method="POST">

      <% function resposta(valor) { return valor || '–'; } %>

      <label>Tipo de visita:</label>
      <div class="resposta-aluno">
        <%= item.tipo_visita === 'espontaneo' ? 'Espontânea' : 'Estimulada' %>
      </div>

      <label>Nome do visitante:</label>
      <div class="resposta-aluno"><%= resposta(item.nome_visitante) %></div>

      <label>Consultora responsável:</label>
      <div class="resposta-aluno"><%= resposta(item.consultora) %></div>

      <label>Data e horário da visita:</label>
      <div class="resposta-aluno"><%= new Date(item.data_horario_visita).toLocaleString('pt-BR') %></div>

      <img src="/assets/2.png" alt="Prioridades" class="imagem">

      <label>Pratica atividade física?</label>
      <div class="resposta-aluno"><%= resposta(item.atividade_fisica) %></div>
      <label>Se sim, qual?</label>
      <div class="resposta-aluno"><%= resposta(item.qual_atividade) %></div>
      <textarea name="obs_atividade_fisica"><%= item.obs_atividade_fisica || '' %></textarea>

      <label>Forma física atual:</label>
      <div class="resposta-aluno"><%= resposta(item.forma_fisica) %></div>
      <textarea name="obs_forma_fisica"><%= item.obs_forma_fisica || '' %></textarea>

      <label>Ativo em outra academia?</label>
      <div class="resposta-aluno"><%= resposta(item.outra_academia) %></div>
      <textarea name="obs_outra_academia"><%= item.obs_outra_academia || '' %></textarea>

      <label>Horários de interesse:</label>
      <div class="resposta-aluno"><%= (item.horario || []).join(', ') || '–' %></div>
      <textarea name="obs_horario"><%= item.obs_horario || '' %></textarea>

      <label>Modalidade de maior interesse:</label>
      <div class="resposta-aluno"><%= resposta(item.modalidade_interesse) %></div>
      <textarea name="obs_modalidade"><%= item.obs_modalidade || '' %></textarea>

      <label>O que é importante para você?</label>
      <div class="resposta-aluno"><%= (item.importancia || []).join(', ') || '–' %></div>
      <textarea name="obs_importancia"><%= item.obs_importancia || '' %></textarea>

      <label>O que te trouxe até aqui?</label>
      <div class="resposta-aluno"><%= (item.motivacao || []).join(', ') || '–' %></div>
      <textarea name="obs_motivo"><%= item.obs_motivo || '' %></textarea>

      <label>Observações gerais:</label>
      <textarea name="observacoes"><%= item.observacoes || '' %></textarea>

      <button type="submit">Salvar alterações</button>
    </form>
  </div>
</body>
</html>
